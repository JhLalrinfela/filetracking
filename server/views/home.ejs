<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link href="css/style.css" rel="stylesheet" />
    <link href="css/home.css" rel="stylesheet" />

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="js/script.js"></script>
  </head>
  <body>
    <div class="container">
      <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <a class="navbar-brand p-2" href="#">ELetterMovement</a>
      </nav>
      <div class="row mt-4">
        <div class="col-md-9" id="secondrow">
          <h3>FILE LEH A CHANGTU TE</h3>
          <div class="table-responsive" id="table">
            <table class="table table-bordered">
              <thead>
                <tr>
                  <th>FIle No.</th>
                  <th>File_Name</th>
                  <th>Changtu</th>
                </tr>
              </thead>
              <tbody>
                <% if(sampleData.length > 0) { sampleData.forEach(function(data)
                { %>
                <tr>
                  <td><%= data.file_no %></td>
                  <td><%= data.file_name %></td>
                  <td><%= data.name %></td>
                </tr>
                <% }); } else { %>
                <tr>
                  <td>No Data Found</td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
        <div class="col">
          <form id="userlogin">
            <h2 class="m-3">User Login</h2>
            <div class="form-group mb-4">
              <input
                type="email"
                class="form-control"
                id="userid"
                aria-describedby="emailHelp"
                placeholder="Enter Id"
              />
            </div>
            <div class="form-group mb-4">
              <input
                type="password"
                class="form-control"
                id="password"
                placeholder="Password"
              />
            </div>
            <button type="button" class="btn btn-primary mb-4" id="login">
              Submit
            </button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
